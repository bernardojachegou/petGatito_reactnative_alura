import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  fill: {
    flex: 1,
  },
});

export const colors = {
  primaryColor: '#A050BE',
  secondaryColor: '#FABE50',
  darkColor: '#555555',
  lightColor: '#fff',
  neutralColor: '#C7C7C7',
};
